# AI Receptionist Demo Scenarios

Synthetic caller scenarios for proving the voice intake offer without live calls or real customer data.

Run them against the dry-run endpoint:

```bash
python3 run_demo_scenarios.py
```

The voice agent writes intake packets under:

`products/Private-AI-Lab/apps/jvt-inbound-voice-agent/data/intake`
